# Leave EMPTY for local development: `npm run dev` proxies /api to the backend
# (see vite.config.js), which avoids CORS and Windows IPv6-localhost issues.
# Set this only for production builds, to your deployed backend's URL, e.g.
# VITE_API_URL=https://api.visionaryx.example.com
VITE_API_URL=
